spring:
  datasource:
    url: jdbc:postgresql://postgres-stage:5432/trading_app_stage
    username: postgres
    password: stage_secure_pass
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: INFO

